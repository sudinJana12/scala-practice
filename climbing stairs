object Solution {
    def climbStairs(n: Int): Int = {
        
        if(n==1)
        1
        else{
          var arr=new Array[Int](n+1);
          arr(0)=1;
          arr(1)=1;
          for(i<-2 to n){
              arr(i)=arr(i-1)+arr(i-2);
          }
          arr(n);
        }
    }
}
